# 🔌 重启后快速接上指南

> 重启 Claude Code 后，新会话开头**复制这段话**粘进去，1 分钟内接上进度。

---

## 📋 重启后第一次对话模板

直接复制下面这段（修改 <> 部分）：

```
我们之前完成了"智能调度模块 v1.1"开发，详情在左脑知识库：
- KB-20260621-013 智能调度MVP完成
- KB-20260621-014 智能调度模块完成v1
- KB-20260621-015 智能调度模块扩展完成
- KB-20260622-001 智能调度模块扩展完成（日期更新）
- KB-20260622-002 权限设置关键洞察

代码位置：
- 规则引擎: scripts/orchestrator/dispatcher.js (12/12 测试通过)
- 钩子入口: scripts/orchestrator/hooks/dispatch-decision.js
- 斜杠命令: .claude/commands/{dispatch,parallel}.md
- 文档: scripts/orchestrator/文档/{usage,PERMISSIONS-SETUP,PERMISSIONS-PATCH}.md
- 真实案例: AI-【3】-项目开发/点餐系统/docs/dish-form-refactor-plan.md

我现在的状态: <说你要继续做什么，例如"测 /dispatch"/"继续调规则"/"接新需求">
```

---

## 🔄 重启后必跑的 3 个验证

```bash
# 1. dispatcher 还能用
node H:/AI-han/AiCode/scripts/orchestrator/dispatcher.js "排查 BUG"

# 2. 12/12 测试还过
node H:/AI-han/AiCode/scripts/orchestrator/test-dispatcher.js

# 3. 加载左脑摘要
bash .claude/skills/left-brain/scripts/session-summary.sh load
```

**期望输出**：
- 测试 1: `{ "dispatch": true, "agents": 2, ... }`
- 测试 2: `📊 测试结果: 12/12 通过, 0 失败`
- 测试 3: 加载之前的会话摘要

---

## 📂 文件位置速查

| 类别 | 路径 |
|:-----|:-----|
| 规则引擎 | `H:/AI-han/AiCode/scripts/orchestrator/dispatcher.js` |
| 测试 | `H:/AI-han/AiCode/scripts/orchestrator/test-dispatcher.js` |
| Layer 2 评分 | `H:/AI-han/AiCode/scripts/orchestrator/llm-scorer.js` |
| 成本监控 | `H:/AI-han/AiCode/scripts/orchestrator/token-monitor.js` |
| 规则学习 | `H:/AI-han/AiCode/scripts/orchestrator/learn-rules.js` |
| 钩子入口 | `H:/AI-han/AiCode/scripts/orchestrator/hooks/dispatch-decision.js` |
| /dispatch 命令 | `H:/AI-han/AiCode/.claude/commands/dispatch.md` |
| /parallel 命令 | `H:/AI-han/AiCode/.claude/commands/parallel.md` |
| 权限配置 | `H:/AI-han/AiCode/.claude/settings.local.json` |
| 决策指南 | `H:/AI-han/AiCode/scripts/orchestrator/决策指南.md` |
| 使用文档 | `H:/AI-han/AiCode/scripts/orchestrator/文档/使用文档.md` |
| 权限指南 | `H:/AI-han/AiCode/scripts/orchestrator/文档/权限设置指南.md` |
| 权限 patch | `H:/AI-han/AiCode/scripts/orchestrator/文档/权限补丁模板.md` |
| 重启指南 | `H:/AI-han/AiCode/scripts/orchestrator/文档/重启指南.md` (本文件) |

---

## 🎯 重启后想做什么

| 你想 | 我会做什么 |
|:-----|:----------|
| **测试智能调度** | 跑 `/dispatch 排查 BUG` 看效果 |
| **调规则** | 改 `dispatcher.js` 里的 `RULES` 对象，跑测试验证 |
| **加新规则** | 我给候选，你手动加 `settings.local.json` |
| **接新需求** | 跑 `dispatcher.js` 决策，按需派 Agent |
| **看历史** | 跑 `left-brain.sh recall "智能调度"` |
| **加 Layer 2 LLM** | 替换 `llm-scorer.js` 的启发式为真实 LLM |
| **加 /parallel 演示** | 跑 `/parallel 3 排查 X` 看强制并行效果 |

---

## ⚠️ 关键提醒

1. **PreToolUse 钩子**：每次你提问时**自动跑**（无需操作）
2. **auto mode 拦截**：AI 不能改自己的 `settings.local.json`（已踩坑）
3. **通配符限制**：避免 `Bash(*)`、`Write(C:/**)` 等危险规则
4. **重启 CC** 才会让新加的权限规则生效

---

## 📞 出问题怎么办

| 现象 | 解决 |
|:-----|:-----|
| 钩子不跑 | 检查 `.claude/settings.local.json` 的 PreToolUse 配置 |
| dispatcher 报错 | 检查 `node` 在 PATH 里 |
| 12/12 失败 | 看具体哪个挂了，单独 debug |
| 权限弹窗太多 | 把频繁的命令加到 `permissions.allow` |
| 找不到 KB | 跑 `bash .claude/skills/left-brain/scripts/left-brain.sh status` |
| /dispatch 不识别 | 重启 CC 或检查 `.claude/commands/dispatch.md` 存在 |

---

## 🎓 今天的成果一句话总结

> **"在 Claude Code 里实现了 2-3 倍提速的智能调度：规则引擎自动判断要不要派子代理，0 Token 决策成本，真实 BUG 排查案例验证有效。"**

下次开会话直接说这句话，AI 立刻知道上下文。
