[KB-20260622-005] save.js bug - 第二次跑时 updateQuickLoad 失败，已手动修复 ROOT_快速加载会话.md。根因：save.js 用 existing.includes('## 📚 快照历史') 判断是否第一次，但 file.replace 写文件后表头已存在但代码判断逻辑有 race condition。修复方案：重写 updateQuickLoad 用更鲁棒的方式（找表头位置+splice 插入），但实际跑仍未生效，原因待查。下次再用 save.js 前先手动检查索引。