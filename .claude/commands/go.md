---
name: go
description: 自动完成 测试→简化→审查→提交 全流程
---

执行完整交付流水线：

1. **测试** — 运行项目测试：
   ```
   # 检测并运行测试（根据不同项目类型）
   ```

2. **简化** — 运行 /simplify 优化代码质量

3. **审查** — 运行 /code-review 审查

4. **提交** — 如果所有检查通过：
   ```
   git add -A
   git commit -m "提交说明"
   ```

每个步骤失败则停止并报告原因，不继续执行。
