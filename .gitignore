# 临时文件
logs/
coverage/
*.log
*.tmp
*.stackdump
.DS_Store
Thumbs.db

# 动态学习数据（每次运行变化，不应入库）
scripts/orchestrator/learned-keywords.json
data/workspace.db
data/github/
data/corpus/

# 编辑器
.vscode/
.idea/
*.swp
*.swo

# 大文件（如果需要）
node_modules/
dist/
build/
target/

# 内部快照（本地会话状态，不进入 git，避免仓库膨胀）
.claude/snapshots/
.claude/skills/left-brain/memory/sessions/snapshots/

# MemOS 动态数据（每日扫描生成）
.claude/skills/left-brain/memory/memos-tier.json
.claude/skills/left-brain/memory/logs/memos-evolution.jsonl

# 自我反思反馈队列（PostToolUse 写入，会话级别）
.claude/skills/left-brain/memory/reflections.jsonl

# 智能任务规划 pending plan（PostToolUse 写入，用户 /ok 或 /no 处理）
.claude/skills/left-brain/memory/pending-plans.json

# 主动扫描异常 + 缓存（SessionStart 写入，每次扫描刷新）
.claude/skills/left-brain/memory/anomalies.json
.claude/skills/left-brain/memory/.last-scan.json

# 语义检索 TF-IDF 索引缓存（每次 KB 变化重建，不入库）
.claude/skills/left-brain/memory/embeddings/

# 自动化修复 proposals（auto-fix 写入，等用户 /ok）
.claude/skills/left-brain/memory/fix-proposals.json

# Plan 执行日志（plan-bridge 写入）
.claude/skills/left-brain/memory/plan-execution-log.json

# 路线图同步日志（sync-roadmap 写入，会话级运行态）
data/roadmap-sync/

# L5 进度同步日志（sync-l5-progress 写入，会话级运行态）
data/l5-sync/

# 自主模式状态（autonomous.js 写入，会话级开关）
.claude/skills/left-brain/memory/autonomous-state.json

# M24-B: handoff 生命周期日志（运行态，会话级）
.claude/skills/left-brain/memory/handoff_lifecycle.jsonl

# 演进计划锁状态（evolution-lock.js 写入，P0-0 元能力，会话级）
.claude/skills/left-brain/memory/evolution-plan.json
.claude/skills/left-brain/memory/evolution-lock-violations.jsonl

# 失败蒸馏器动态数据（M13 v2.0.5）
.claude/skills/left-brain/memory/distillation-log.jsonl
.claude/skills/left-brain/memory/knowledge/KB-*.md

# 左脑自动索引（session-summary.sh 自动重写）
.claude/skills/left-brain/memory/MEMORY.md
.claude/skills/left-brain/memory/associations/graph.json

# 会话快照与状态（session-summary.sh / state-snapshot.js 自动生成）
.claude/skills/left-brain/memory/sessions/latest_state.json
.claude/skills/left-brain/memory/sessions/latest_summary.md
.claude/skills/left-brain/memory/sessions/session_*.md
00_ROOT_快速加载会话.md

# 自主模式决策记录（autonomous.js 写入）
.claude/skills/left-brain/memory/autonomous-decision-*.json

# cron 报告（每日/每周自动生成）
.claude/skills/left-brain/memory/cron-reports.json

# 会话级快照模式覆盖（/snap-mode 写入，会话结束可清）
.claude/session-snap-mode.json

# 个人 workflow 智能化动态数据（v2.0 P0-5）
.claude/skills/left-brain/memory/workflow-events.jsonl
.claude/skills/left-brain/memory/workflow-patterns.json
scripts/orchestrator/workflow/.test-*/

# Claude HUD 插件本地配置
.claude/settings.json

# 工程审计（/audit）动态数据（v2.0.2 P0-6）
.claude/audits/
.claude/skills/left-brain/memory/audit-history.json

# 左脑孤儿目录（路径错乱时的空壳 sessions/，2026-06-25）
.claude/skills/left-brain/scripts/memory/

# handoff 临时接续 prompt（每次 --auto 运行时生成，不入库）
.claude/handoff/
!.claude/handoff/TUTORIAL.md

# M15 评价闭环产物（v2.0.6）月度报告
# 本地存在即可（跨会话趋势靠 database 聚合），不入库
data/evolution/metrics-*.md
data/evolution/auto-tasks.json

# M16 候选汇聚桥梁产物（v3.0.1）
data/bridge/

# M36A ui-skill-installer 离线缓存（每次扫描 GitHub 刷新，不入库）
.claude/skills/ui-skill-installer/memory/

# M36B skill-registry 离线缓存 + 运行时状态（每次 install 变化，不入库）
data/skill-registry/
