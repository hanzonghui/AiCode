---
name: evolve
displayName: 🧬 自我进化 — 从 GitHub 学新能力
version: 1.0
description: >
  从 GitHub Trending + Search 扫描 Claude 相关爆款项目，TF-IDF + LLM-judge 评估
  可行性，自动入队高价值候选到 evolution-plan.json，让 AiCode 持续长新技能。
tags:
  - evolution
  - github-scanner
  - candidate-evaluation
  - auto-implement
  - llm-judge
author: 韩宗辉
icon: 🧬
gepa:
  enabled: true
  optimize_target: body
  eval_dataset_source: synthetic
  eval_dataset: data/gepa/evolve/eval-dataset.json
  max_iterations: 5
  population_size: 8
  fitness_weights:
    clarity: 0.30
    coverage: 0.25
    error_reduction: 0.25
    size_eff: 0.20
---

# 🧬 自我进化 Skill

> **v1.0 · GitHub 扫描 · 候选评估 · 自动实现 · LLM-judge 双轨制**

---

## ⚡ 30 秒上手

```bash
/evolve scan         # 扫描 GitHub 爆款 Claude 项目
/evolve analyze      # TF-IDF + LLM-judge 评估候选
/evolve run          # 扫描 + 分析完整流程
/evolve candidates   # 查看当前候选列表
/evolve implement N  # 实现第 N 个候选（自动分支+prompt+测试）
/evolve status       # 查看已实现特性
/evolve log          # 进化历史
/evolve watch        # 自动判断感知层（每日/每周/月度）
/evolve report       # 趋势报告
```

---

## 🎯 核心能力

### 1. 三层感知

| 层级 | 频率 | 作用 |
|:-----|:-----|:-----|
| **每日** | cron / SessionStart | 抓新 trending、检查已实现特性是否过时 |
| **每周** | cron | 周度趋势对比 + 新增爆款项目 |
| **月度** | cron | 30 天积累复盘 + 报告 |

### 2. 候选评估（双轨制 M12）

| 维度 | 自动判断 | 评分 |
|:-----|:---------|:----:|
| **实用性** | TF-IDF 关键词相关度 | 1-10 |
| **可行性** | 人手/技术栈匹配 | 1-10 |
| **独立性** | 是否需要外部依赖 | 1-10 |
| **风险度** | 改动范围 + 安全影响 | 1-10（反向）|
| **新鲜度** | 发布/更新时间 | 1-10 |

**采纳标准**：综合分 ≥ 7.0 标 `suggestion=adopt`，自动入 `evolution-plan.json next`。

### 3. LLM-judge 闸门（M12）

- `judgeCandidateWithFallback()`：4 adapter 镜像
- **双轨制**：LLM reject 一票否决 + LLM accept 走硬阈值兜底
- 避免 AI 误判，测试 52/52 通过

### 4. 自动实现（M7 · 5 道安全闸门）

```
候选入队 → 分支 → prompt 生成 → Claude 实现 → 测试 → 审查 → 合并
                                ↑                    ↓
                                └── 5 道闸门: ──┘
                                    - 失败保护
                                    - 干跑模式
                                    - 测试覆盖率
                                    - LLM-judge
                                    - 二次采样（M9）
```

---

## 🔧 核心脚本

| 文件 | 作用 |
|:-----|:-----|
| `scripts/evolution/daily-evolution.js` | 主入口（scan/analyze/run/candidates/implement/status/log/watch/report）|
| `scripts/evolution/github-scanner.js` | GitHub Trending + Search API（含 token 认证 M18）|
| `scripts/orchestrator/llm-adapter.js` | LLM-judge 双轨制（M12 judgeCandidateWithFallback）+ 4 个 adapter 镜像|
| `scripts/evolution/auto-implement.js` | 自动实现引擎（M7，含 5 道闸门）|
| `scripts/orchestrator/llm-adapter.js` | LLM 调用 adapter（4 个镜像）|

## 📊 数据流

```
GitHub API
  ↓ (token 认证 M18)
github-scanner.js
  ↓ (TF-IDF 打分)
judgeCandidateWithFallback() (in llm-adapter.js)
  ↓ (suggestion=adopt)
data/github/candidates.json
  ↓ (bridge M16)
evolution-plan.json next 队列
  ↓ (auto-implement M7)
新代码 + 测试 + commit
```

## ⚠️ 安全约束

- ✅ 自主做：扫描 + 评估 + 入队 + 自动实现（带 5 道闸门）
- ⚠️ 慎做：合并到 main（先 dry-run，看 diff 再决定）
- ❌ 不做：push 到远程 / 删分支 / 改主工作目录外文件

## 🔗 关联

- 命令入口：`.claude/commands/evolve.md`
- 队列：`evolution-plan.json`（gitignore）
- 评估标准：见 `data/github/candidates.json` schema
- L5 影响：第 4 条（月度 metric 报告）的候选来源

---

## 🧪 测试

- `scripts/evolution/test-github-scanner-auth.js`：14/14 通过（M18 token 认证）
- `scripts/evolution/test-auto-implement.js`：27/27 通过（M7 5 道闸门）
- `scripts/orchestrator/test-judge-candidate.js`：52/52 通过（M12 双轨制）
- 全量回归：318+ 测试

---

*升级自 `/evolve` 命令（v1.0 · 2026-06-27 · M25 skill 生态扩展）*